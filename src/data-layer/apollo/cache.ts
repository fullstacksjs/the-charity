import { InMemoryCache } from '@apollo/client';

// const possibleTypes = {
//   // eslint-disable-next-line @typescript-eslint/naming-convention
//   Family: ['CompletedFamily', 'DraftFamily'],
// };

export const cache = new InMemoryCache();
